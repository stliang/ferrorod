import React from 'react';
import withRoot from '../../withRoot';

const Notify = () => {
  return (
    <React.Fragment>
      <h1>Notify</h1>
    </React.Fragment>
  );
}

export default withRoot(Notify);