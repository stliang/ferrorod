import { fields } from '../../../config'

export const addDialogUIConfig = {
    textFields: fields,
    title: 'Add Asset'
}

export const initialValue = {
    asset: '',
    cost: 0,
    price: 0,
    quantity: 0,
    status: '',
    subRows: undefined
}